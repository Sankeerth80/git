services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - '3000:3000'
    environment:
      - MONGODB_URI=${MONGODB_URI}
      - JWT_SECRET=${JWT_SECRET}
      - ADMIN_USERNAME=${ADMIN_USERNAME:-Sankeerth}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-Satyamani80}
